import {DocArgsTable, DocCanvas} from '@pine-ds/doc-components';
import { components } from '../../../../dist/docs.json';

# Button

A button is an interactive element that can be clicked to perform an action or submit a form.

## Guidelines

### When to use

- Communicating an action that will occur (e.g. saving a page).
- Triggering or enabling an action, such as submitting requested information (e.g. forms).
- Progressing or regressing a user through a step in a flow (e.g. modals).

### When not to use

- Directing users to a new page or different part within the same page. Instead, use [Link](/docs/components-link--external).
- When space is limited consider an IconButton.

## Accessibility

While accessible out of the box, there are some best practices to keep in mind when using the component:

- Buttons should always contain helpful text about what they do even if this text is expected to be visually hidden. (e.g. "submit form" or "delete account").
- Buttons should not disable tabbing or keyboard navigation.
- Buttons should always pass [color contrast best practices](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html).

## Properties

<DocArgsTable componentName="pds-button" docSource={components}/>

## Variant

### Primary

The default button, intended to be used with affirmative actions such as submitting, continuing, and saving.

<DocCanvas client:only
  mdxSource={{
    react: `
    <PdsButton>Primary</PdsButton>
    <PdsButton disabled="true">Disabled</PdsButton>
    <PdsButton icon="trash">Icon</PdsButton>
    `,
    webComponent: `
    <pds-button>Primary</pds-button>
    <pds-button disabled="true">Disabled</pds-button>
    <pds-button icon="trash">Icon</pds-button>
    `
}}>
  <pds-button>Primary</pds-button>
  <pds-button disabled="true">Disabled</pds-button>
  <pds-button icon="trash">Icon</pds-button>
</DocCanvas>

### Secondary

Secondary button variant, intended to be used as back buttons, for saving drafts, or cancelling actions.

<DocCanvas client:only
  mdxSource={{
    react: `
    <PdsButton variant="secondary">Secondary</PdsButton>
    <PdsButton variant="secondary" disabled="true">Disabled</PdsButton>
    <PdsButton variant="secondary" icon="trash">Icon</PdsButton>
    `,
    webComponent: `
    <pds-button variant="secondary">Secondary</pds-button>
    <pds-button variant="secondary" disabled="true">Disabled</pds-button>
    <pds-button variant="secondary" icon="trash">Icon</pds-button>
    `
}}>
  <pds-button variant="secondary">Secondary</pds-button>
  <pds-button variant="secondary" disabled="true">Disabled</pds-button>
  <pds-button variant="secondary" icon="trash">Icon</pds-button>
</DocCanvas>

### Accent

Accent button variant, intended to be used for tertiary actions or when higher contrast is needed.

<DocCanvas client:only
  mdxSource={{
    react: `
    <PdsButton variant="accent">Accent</PdsButton>
    <PdsButton variant="accent" disabled="true">Disabled</PdsButton>
    <PdsButton variant="accent" icon="trash">Icon</PdsButton>
    `,
    webComponent: `
    <pds-button variant="accent">Accent</pds-button>
    <pds-button variant="accent" disabled="true">Disabled</pds-button>
    <pds-button variant="accent" icon="trash">Icon</pds-button>
    `
}}>
  <pds-button variant="accent">Accent</pds-button>
  <pds-button variant="accent" disabled="true">Disabled</pds-button>
  <pds-button variant="accent" icon="trash">Icon</pds-button>
</DocCanvas>

### Disclosure

Disclosure button variant, intended to be used as a trigger to show or hide content elsewhere on the page.

**Note**: Icon cannot be customized.

<DocCanvas client:only
  mdxSource={{
    react: `
    <PdsButton variant="disclosure">Disclosure</PdsButton>
    <PdsButton variant="disclosure" disabled="true">Disabled</PdsButton>
    `,
    webComponent: `
    <pds-button variant="disclosure">Disclosure</pds-button>
    <pds-button variant="disclosure" disabled="true">Disabled</pds-button>
    `
}}>
  <pds-button variant="disclosure">Disclosure</pds-button>
  <pds-button variant="disclosure" disabled="true">Disabled</pds-button>
</DocCanvas>

### Destructive

Destructive button variant, intended to be used with destructive actions, such as deleting an item or removing a contact.

<DocCanvas client:only
  mdxSource={{
    react: `
    <PdsButton variant="destructive">Destructive</PdsButton>
    <PdsButton variant="destructive" disabled="true">Disabled</PdsButton>
    <PdsButton variant="destructive" icon="trash">Icon</PdsButton>
    `,
    webComponent: `
    <pds-button variant="destructive">Destructive</pds-button>
    <pds-button variant="destructive" disabled="true">Disabled</pds-button>
    <pds-button variant="destructive" icon="trash">Icon</pds-button>
    `
}}>
  <pds-button variant="destructive">Destructive</pds-button>
  <pds-button variant="destructive" disabled="true">Disabled</pds-button>
  <pds-button variant="destructive" icon="trash">Icon</pds-button>
</DocCanvas>

## Type

The type prop allows a `pds-button` to set a [button type attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type).

### Default (Button)

The button has no default click behavior. It can have client-side scripts listen to the element's events, which are triggered when the events occur.

### Submit

The button submits the form data to the server.

### Reset

The button resets all the controls to their initial values. [As noted on MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type), this behavior tends to annoy users.

## Additional Resources

[CSS Tricks: Buttons vs. Links](https://css-tricks.com/buttons-vs-links/)

[MDN: Button Element](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button)
