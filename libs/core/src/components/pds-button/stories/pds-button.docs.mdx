import { Meta, Canvas, ArgTypes } from '@storybook/blocks';

import * as stories from './pds-button.stories.js';

<Meta of={stories} />

# Button

A button component to generate all versions of `pds-button` as outlined in [Figma](https://www.figma.com/file/sMbtLUHSt2vfKgKjyQ3052/%F0%9F%A7%A9-Sage-components?node-id=3%3A15&t=dIcvX1VtOu0FL0VE-0).

## Usage Guidelines

### When to use

- Communicating an action that will occur. (e.g. saving a page)
- Triggering or enabling an action, such as submitting requested information. (e.g. forms)
- Progressing or regressing a user through a step in a flow. (e.g. modals)

### When not to use

- Directing users to a new page or different part within the same page. Instead, use [Link](/docs/components-link--external).
- When space is limited consider an IconButton.

## Accessibility

When following our usage guidelines, pds-button should be accessible right out of the box but there are still a few general accessibility best practices to make sure you follow:

- Buttons should always contain helpful text about what they do even if this text is expected to be visually hidden. (e.g. "submit form" or "delete account")
- Buttons should not disable tabbing or keyboard navigation.
- Buttons should always pass [color contrast best practices](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html).

## Properties

<ArgTypes of={stories} />

## Variant

### Default (Primary)

Default button styles, used with affirmative actions such as submitting, continuing, and saving

<Canvas mdxSource={`
<pds-button>Default</pds-button>
<pds-button disabled="true">Disabled</pds-button>
<pds-button icon="trashIcon">Icon</pds-button>
`}>
  <pds-button>Default</pds-button>
  <pds-button disabled="true">Disabled</pds-button>
  <pds-button icon="trashIcon">Icon</pds-button>
</Canvas>

### Secondary

Secondary button styles, used with neutral actions such as going back, saving as draft, and cancelling

<Canvas mdxSource={`
<pds-button variant="secondary">Secondary</pds-button>
<pds-button variant="secondary" disabled="true">Disabled</pds-button>
<pds-button variant="secondary" icon="trashIcon">Icon</pds-button>
`}>
  <pds-button variant="secondary">Secondary</pds-button>
  <pds-button variant="secondary" disabled="true">Disabled</pds-button>
  <pds-button variant="secondary" icon="trashIcon">Icon</pds-button>
</Canvas>

### Accent

Accent button styles, used for tertiary actions or when more contrast is needed

<Canvas mdxSource={`
<pds-button variant="accent">Default</pds-button>
<pds-button variant="accent" disabled="true">Disabled</pds-button>
<pds-button variant="accent" icon="trashIcon">Icon</pds-button>
`}>
  <pds-button variant="accent">Default</pds-button>
  <pds-button variant="accent" disabled="true">Disabled</pds-button>
  <pds-button variant="accent" icon="trashIcon">Icon</pds-button>
</Canvas>

### Disclosure

Disclosure button styles, used to provide multiple options or more information within a button

<Canvas mdxSource={`
<pds-button variant="disclosure">Default</pds-button>
<pds-button variant="disclosure" disabled="true">Disabled</pds-button>
<pds-button variant="disclosure" icon="trashIcon">Icon</pds-button>
`}>
  <pds-button variant="disclosure">Default</pds-button>
  <pds-button variant="disclosure" disabled="true">Disabled</pds-button>
  <pds-button variant="disclosure" icon="trashIcon">Icon</pds-button>
</Canvas>

### Destructive

Destructive button styles, used with actions that delete information such as deleting an item or removing a contact

<Canvas mdxSource={`
<pds-button variant="destructive">Default</pds-button>
<pds-button variant="destructive" disabled="true">Disabled</pds-button>
<pds-button variant="destructive" icon="trashIcon">Icon</pds-button>
`}>
  <pds-button variant="destructive">Default</pds-button>
  <pds-button variant="destructive" disabled="true">Disabled</pds-button>
  <pds-button variant="destructive" icon="trashIcon">Icon</pds-button>
</Canvas>

## Type

The type prop allows a `pds-button` to set [button type attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type).

### Default (Button)

The button has no default behavior, and does nothing when pressed by default. It can have client-side scripts listen to the element's events, which are triggered when the events occur.

This is our default button type.

### Submit

The button submits the form data to the server.

### Reset

The button resets all the controls to their initial values. [As noted on MDN, this behavior tends to annoy users](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type).

## Additional Resources

[CSS Tricks - Buttons vs. Links](https://css-tricks.com/buttons-vs-links/)
