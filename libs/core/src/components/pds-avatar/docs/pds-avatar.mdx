import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as stories from '../stories/pds-avatar.stories.js';

import {DocArgsTable, DocCanvas} from '@pine-ds/doc-components';
import { components } from '../../../../dist/docs.json';

import sampleImg from '../assets/demi_wilkinson.jpg';

<Meta of={stories} />

# Avatar

The avatar component is used to display a thumbnail representation of an admin, customer, or business.

## Guidelines

### When to use
- Use the `Avatar` to visually represent a user or entity, such as a profile picture, admin, or customer.
- Ideal for applications where identifying users or roles with a small image is needed.

### When not to use
- Avoid using for large images or media that require detailed visibility.
- Do not use for items that need a descriptive label or detailed information beyond a simple visual representation.

### Accessibility
- Always include an `alt` text description for profile images, ensuring users with visual impairments understand the context.

## Properties

<DocArgsTable componentName="pds-avatar" docSource={components} />

## Examples

### Default

The default avatar style, typically used to represent a generic user or business.

<DocCanvas
  mdxSource={{
    react: '<PdsAvatar></PdsAvatar>',
    webComponent: '<pds-avatar></pds-avatar>'
}}>
  <pds-avatar></pds-avatar>
</DocCanvas>

### Admin

An avatar variant specifically for admins, featuring less-rounded borders for distinction.

<DocCanvas
  mdxSource={{
    react: '<PdsAvatar variant="admin"></PdsAvatar>',
    webComponent: '<pds-avatar variant="admin"></pds-avatar>'
}}>
  <pds-avatar variant="admin"></pds-avatar>
</DocCanvas>

### Badge

An avatar that displays a badge in addition to the profile image.

<DocCanvas
  mdxSource={{
    react: '<PdsAvatar badge={true}></PdsAvatar>',
    webComponent: '<pds-avatar badge="true"></pds-avatar>'
}}>
  <pds-avatar badge="true"></pds-avatar>
</DocCanvas>

### Custom Sizes

An avatar where the size can be customized through the `size` prop.

<DocCanvas
  mdxSource={{
    react: '<PdsAvatar size="128px"></PdsAvatar>',
    webComponent: '<pds-avatar size="128px"></pds-avatar>'
}}>
  <pds-avatar size="128px"></pds-avatar>
</DocCanvas>

### Dropdown

Allows the avatar to be used as a dropdown trigger.

<DocCanvas
  mdxSource={{
    react: '<PdsAvatar dropdown={true}></PdsAvatar>',
    webComponent: '<pds-avatar dropdown="true"></pds-avatar>'
}}>
  <pds-avatar dropdown="true"></pds-avatar>
</DocCanvas>

### Image

An avatar that displays a user's profile image. If no image is provided, a default user icon is shown.

<DocCanvas
  mdxSource={{
    react: '<PdsAvatar alt="Customer Profile" image="../assets/demi_wilkinson.jpg"></PdsAvatar>',
    webComponent: '<pds-avatar alt="Customer Profile" image="../assets/demi_wilkinson.jpg"></pds-avatar>'
}}>
  <pds-avatar alt="Customer Profile" image={sampleImg}></pds-avatar>
</DocCanvas>

## Playground

<Canvas of={stories.Default} />

<Controls of={stories.Default} />
