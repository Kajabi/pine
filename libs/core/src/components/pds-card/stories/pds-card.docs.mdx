import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import * as stories from './pds-card.stories.js';

<Meta of={stories} />

# Card

A block container that provides styling for elevation, border, and background.

## Properties

<ArgTypes of={stories} />

### Default

<Canvas mdxSource={`<pds-card>Card content</pds-card>`}>
  <pds-card>Card content</pds-card>
</Canvas>

### Padding

Padding is the space between a card's content and its border.

<Canvas mdxSource={`<pds-card padding="sm">Card content</pds-card>`}>
  <pds-card padding="sm">Card content</pds-card>
</Canvas>

### Border

A border is an outline that displays around a card and is visible by default.

Set `border="false"` to remove the border.

<Canvas mdxSource={`<pds-card border="false">Card content</pds-card>`}>
  <pds-card border="false">Card content</pds-card>
</Canvas>

### Background Color

To change a card's appearance, apply a background color using the `bg-color` prop.

When using a background color, it is recommended to also use `border="false"` in order to improve the overall appearance.

***Note:*** `bg-color` can accept the following color syntax types:

- Hexadecimal: `#ff0000`
- RGB: `rgb(255, 0, 0)`
- RGBA: `rgba(255, 0, 0, 0.3)`
- HSL: `hsl(120, 100%, 50%)`
- HSLA: `hsla(120, 100%, 50%, 0.3)`
- Named:  `red`, `orange`, `yellow`
- Custom properties: `var(--pine-color-primary-100)`
- Keyword: `currentcolor`

<Canvas mdxSource={`<pds-card border="false" bg-color="var(--pine-color-primary-100)">Card content</pds-card>`}>
<pds-card border="false" bg-color="var(--pine-color-primary-100)">Card content</pds-card>
</Canvas>

### Shadow

Adds a shadow effect to the card, which can be used to create the appearance of elevation.

<Canvas mdxSource={`<pds-card shadow="lg">Card content</pds-card>`}>
  <pds-card shadow="lg">Card content</pds-card>
</Canvas>
