import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { html, render } from 'lit-html';

import { extractArgTypes } from '@pxtrn/storybook-addon-docs-stencil';

<Meta title="webcomponents/Button" component="sage-button" argTypes={extractArgTypes('sage-button')} />

export const Template = (args) =>
  html` <sage-button value=${args.value} variant=${args.variant} name=${args.name} icon=${args.icon} disabled=${args.disabled} type=${args.type}> ${args.slot} </sage-button> `;

# sage-button

A button component to generate all versions of `sage-button` as outlined in [Figma](https://www.figma.com/file/sMbtLUHSt2vfKgKjyQ3052/%F0%9F%A7%A9-Sage-components?node-id=3%3A15&t=dIcvX1VtOu0FL0VE-0).

## Usage Guidelines

### When to use

- Communicating an action that will occur. (e.g. saving a page)
- Triggering or enabling an action, such as submitting requested information. (e.g. forms)
- Progressing or regressing a user through a step in a flow. (e.g. modals)

### When not to use

- Directing users to a new page or different part within the same page. Instead, use [sage-link](/docs/webcomponents-sage-link--external).
- When space is limited consider an IconButton.

## Accessibility

When following our usage guidelines, sage-button should be accessible right out of the box but there are still a few general accessibility best practices to make sure you follow:

- Buttons should always contain helpful text about what they do even if this text is expected to be visually hidden. (e.g. "submit form" or "delete account")
- Buttons should not disable tabbing or keyboard navigation.
- Buttons should always pass [color contrast best practices](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html).

## Properties


<Canvas>
  <Story name="Default" args={{ icon: '', disabled: false, slot: 'Default', type: 'button' }}>
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable of="sage-button" />

## Variant

### Default (Primary)

Default button styles, used with affirmative actions such as submitting, continuing, and saving

<Canvas withSource="none">
  <sage-button>Default</sage-button>
  <sage-button disabled="true">Disabled</sage-button>
  <sage-button icon="trashIcon">Icon</sage-button>
</Canvas>

### Secondary

Secondary button styles, used with neutral actions such as going back, saving as draft, and cancelling

<Canvas withSource="none">
  <sage-button variant="secondary">Default</sage-button>
  <sage-button variant="secondary" disabled="true">Disabled</sage-button>
  <sage-button variant="secondary" icon="trashIcon">Icon</sage-button>
</Canvas>

### Accent

Accent button styles, used for tertiary actions or when more contrast is needed

<Canvas withSource="none">
  <sage-button variant="accent">Default</sage-button>
  <sage-button variant="accent" disabled="true">Disabled</sage-button>
  <sage-button variant="accent" icon="trashIcon">Icon</sage-button>
</Canvas>

### Disclosure

Disclosure button styles, used to provide multiple options or more information within a button

<Canvas withSource="none">
  <sage-button variant="disclosure">Default</sage-button>
  <sage-button variant="disclosure" disabled="true">Disabled</sage-button>
  <sage-button variant="disclosure" icon="trashIcon">Icon</sage-button>
</Canvas>

### Destructive

Destructive button styles, used with actions that delete information such as deleting an item or removing a contact

<Canvas withSource="none">
  <sage-button variant="destructive">Default</sage-button>
  <sage-button variant="destructive" disabled="true">Disabled</sage-button>
  <sage-button variant="destructive" icon="trashIcon">Icon</sage-button>
</Canvas>

## Type

The type prop allows a `sage-button` to set [button type attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type).

### Default (Button)

The button has no default behavior, and does nothing when pressed by default. It can have client-side scripts listen to the element's events, which are triggered when the events occur.

This is our default button type.

### Submit

The button submits the form data to the server.

### Reset

The button resets all the controls to their initial values. [As noted on MDN, this behavior tends to annoy users](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-type).

## Additional Resources

[CSS Tricks - Buttons vs. Links](https://css-tricks.com/buttons-vs-links/)
