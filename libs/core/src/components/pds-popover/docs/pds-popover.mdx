import { DocArgsTable, DocCanvas } from '@pine-ds/doc-components';
import { components } from '../../../../dist/docs.json';

# Popover
<pds-popover component-id="my-popover" text="Show popover">
    <p>Popover content</p>
    <p>Popover content</p>
    <button popovertarget="mypopover" popovertargetaction="hide"
        onClick={() => {
      const popover = document.getElementById('my-popover');
      if (popover) {
        popover.dispatchEvent(new CustomEvent('hidePdsPopover'));
      }
    }}>
      Hide popover
    </button>
    <pds-button variant="secondary" popovertarget="mypopover" popovertargetaction="hide">
      Hide popover
    </pds-button>
</pds-popover>