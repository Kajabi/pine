import { Meta } from '@storybook/addon-docs/blocks';

import { PatternCatalog } from '../_helpers/PatternCatalog';

<Meta title="Resources/Patterns" parameters={{ previewTabs: { canvas: { hidden: true } } }} />

# Admin composition patterns

Verified page-level Pine recipes aligned with **kajabi-products** admin composition (expert agents, commerce forms, workflows filters, delete modals, referrals empty states). Use these when assembling or migrating Kajabi admin screens — not when looking up individual component props.

> **Source of truth:** `libs/core/src/stories/resources/mcp-patterns.json` is consumed by Storybook (this page) and by [pine-mcp](https://github.com/Kajabi/pine-mcp) (`list_pine_patterns` / `get_pine_pattern`). Edit the JSON in Pine; do not duplicate recipes in pine-mcp.

<PatternCatalog />
