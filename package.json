{
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "engines": {
    "node": ">= 16"
  },
  "scripts": {
    "build.all": "nx run-many --target=build",
    "commit": "npx cz",
    "coverage": "open libs/core/coverage/lcov-report/index.html",
    "stencil.generate": "nx run core:generate",
    "lint.all": "nx run-many --target=lint",
    "prepare": "husky install",
    "setup": "npm install",
    "start": "nx run core:start",
    "test.all": "nx run-many --target=test"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.1.2",
    "@commitlint/config-conventional": "^17.1.0",
    "commitizen": "^4.2.5",
    "husky": "^8.0.1",
    "lerna": "^7.0.1",
    "lint-staged": "^13.0.3",
    "nx": "16.6.0",
    "semver": "^7.3.8",
    "validate-branch-name": "^1.3.0",
    "nx-cloud": "latest"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
